import React from 'react'
import { NavLink } from 'react-router-dom'

const SignedOutLinks = () => {
    return(
        <React.Fragment>
            {/* <NavLink to='/signin' className='navbar-item'><i className="fas fa-sign-in-alt"></i>ログイン</NavLink> */}
        </React.Fragment>
    )
}

export default SignedOutLinks;